#include<stdio.h>
#include<conio.h>

void main()
{
	char 'a'
		printf("enter a alp char:");
	scanf("%c",&a");

	switch(ch)
	{
        case 'a':
        case 'e':
        case 'i':
        case 'o':
        case 'u':
	 printf("%c is a vowel.\n", ch);
            break;
        default:
           
            if (ch >= 'a' && ch <= 'z')
	    {
                printf("%c is a consonant.\n", ch);
            } else {
                printf("%c is not an alphabet character.\n", ch);
            }
            break;
}
}
